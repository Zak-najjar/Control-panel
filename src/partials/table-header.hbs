    <header class="c-table__header">
      <h4 class="c-table__title">{{title}}</h4>
      <div class="c-table__actions">
        {{> @partial-block}}
      </div>
      {{> table-navigation caption=caption}}
    </header>
